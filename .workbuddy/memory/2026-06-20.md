# 2026-06-20

## BugFix: `_parse_push_error` 对 `fetch first` 推送错误匹配缺失
- **问题**：GitHubSync 推送遇到 `[rejected] main -> main (fetch first)` 时，`_parse_push_error()` 只匹配 `non-fast-forward`，遗漏了 `fetch first` 变体，导致显示「未知错误」
- **修复**：`src/git_manager.py:445` — 扩展条件为 `if "rejected" in m and ("non-fast-forward" in m or "fetch first" in m)`
- **验证**：QA 编写 19 个参数化测试用例全部通过，覆盖 fetch-first 新增/回归/兜底/大小写
- **附带发现**：`timeout` 匹配无法捕获 `timed out`（带空格），已记录为非本次变更的已知问题
- **更新日志**：创建 changelog.md（修复分类），同步更新 AGENTS.md（`_parse_push_error` 匹配规则 + 测试说明）
